#include <bits/stdc++.h>

using namespace std;

int main()
{
	int t, n, a;
	
	ios_base::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);
	
	cin >> t;
	while(t--){
		cin >> n;
		while(n--) cin >> a;
		
		cout << a << '\n';
	}
	
	return 0;
}
